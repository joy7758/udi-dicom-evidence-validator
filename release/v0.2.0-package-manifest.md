# v0.2.0 Package Manifest

Included:

- v0.2 profile in `spec/` and `docs/spec/`
- v0.2 manifest and receipt schemas
- public synthetic v0.2 examples under `examples/public/v0.2/`
- deterministic receipt `trace_id` and `provenance` fields
- `trace_id_consistency` and `fdo_mapping_consistency` checks
- v0.2 portable ultrasound demo output under `artifacts_v0.2/`
- updated API metadata, OpenAPI note, agent entrypoints, and boundary docs

Excluded:

- PHI and real samples
- private conformance cases
- clinical validation
- regulatory approval
- certification
- production FDO implementation
